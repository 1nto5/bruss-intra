# bruss-intra environment variables
# Copy to .env.production and fill in real values
#
# Plant legend:
#   [ALL]      — Required for all plants
#   [MRG]      — Required only for mrg (Mrągowo)
#   [MRG-only] — Feature disabled on bri, variable ignored
#   [optional]  — Has sensible default, override if needed

# ===========================================
# Core [ALL]
# ===========================================

NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=    # Generate: openssl rand -base64 32
DEFAULT_LOCALE='pl'
PLANT='mrg'                            # 'mrg' (full access) | 'bri' (DMCheck + employee mgmt + configs only)
DATE_TIME_LOCALE='pl-PL'

# ===========================================
# Database [ALL]
# ===========================================

MONGO_URI="mongodb://127.0.0.1/next_bruss"

# ===========================================
# Auth — NextAuth [ALL]
# ===========================================

AUTH_SECRET=""                          # Generate: openssl rand -hex 16
AUTH_URL="http://localhost:3000"        # Production: http://<server-ip>

# ===========================================
# LDAP [ALL] — used for management login
# ===========================================

# Uncomment the correct line for your plant:
# LDAP="ldap://10.21.10.171:3268"      # BRI (Brieselang / HOI)
LDAP="ldap://10.27.10.171:3268"        # MRG (Mrągowo)
LDAP_DN="CN=NextAuth,OU=SERVICEUSER,OU=_BRUSS-GROUP,DC=bruss-group,DC=com"
LDAP_PASS=""
LDAP_BASE_DN='DC=bruss-group,DC=com'

# ===========================================
# Email [ALL] — notifications, deviation emails, overtime emails
# ===========================================

SMTP_HOST=""                            # e.g. 10.21.10.241
SMTP_PORT="26"
GMAIL_USER=""                           # [optional] Fallback mailer for dev mode
GMAIL_PASS=""                           # [optional] Gmail app password
ADMIN_EMAIL=""                          # [optional] Production only: receives a copy of every sent email

# ===========================================
# Internal API [ALL]
# ===========================================

API="http://localhost:3000/api/"
BASE_URL="http://localhost:3000"        # Used in email links (deviations, overtime)

# ===========================================
# File Uploads [MRG-only] — deviations, overtime attachments
# ===========================================
# BRI has no features that use file uploads.

UPLOAD_BASE_PATH=D:\uploaded_files      # Windows path on production

# ===========================================
# Service Desk KPI [MRG-only]
# ===========================================
# Only used by the /service-desk-kpi route (disabled on bri).

# SERVICEDESK_API_URL=""
# SERVICEDESK_API_KEY=""

# ===========================================
# Power BI [optional]
# ===========================================

# POWERBI_CSV_PATH=""                   # Path to cron-generated CSV

# ===========================================
# Tuning [optional]
# ===========================================

# NEXT_PUBLIC_CHRISTMAS_MODE=false
# NEXT_PUBLIC_LOCALE='pl-PL'
# SESSION_ROLES_REFRESH_INTERVAL=3600000  # ms, default: 1 hour
